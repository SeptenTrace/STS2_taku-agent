{
  "name": "sts2-taku-agent-cli",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "sts": "node --experimental-strip-types tools/sts-cli/main.ts",
    "check": "tsc --noEmit -p tsconfig.json",
    "test:cli": "node --experimental-strip-types --test tools/sts-cli/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
